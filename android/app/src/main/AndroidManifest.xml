<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      No <uses-permission android:name="android.permission.INTERNET" /> on purpose.
      The calculator is fully self-contained and the page states that nothing is
      stored and nothing is transmitted; withholding the permission makes that
      guarantee enforceable by the operating system rather than a claim in prose.

      The merged manifest is not empty of permissions: androidx.core adds
      <package>.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION, a signature permission
      it defines inside this application's own package to guard the non-exported
      receivers it registers at runtime. The app both defines and holds it, so it
      grants nothing to or from anything else. CI asserts that nothing outside
      this package's namespace is ever requested.
    -->

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.IvfTriage">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|uiMode|density|fontScale"
            android:enableOnBackInvokedCallback="true"
            android:exported="true"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
